# Used by api/ai-proxy.ts (and the vite dev middleware) to call OpenRouter
# server-side for the AI support widget. Get a free key at openrouter.ai/keys.
# Only free-tier models (e.g. nvidia/nemotron-nano-9b-v2:free) are used.
OPENROUTER_API_KEY=sk-or-...
