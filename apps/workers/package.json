{
	"name": "otter-workers",
	"private": true,
	"version": "0.1.0",
	"description": "Background job processor. Currently: web-crawl (fetch a URL via Firecrawl, chunk it, embed each chunk, store it in otter-db as a searchable knowledge doc).",
	"type": "module",
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"start": "bun run src/index.ts",
		"test": "bun test"
	},
	"dependencies": {
		"bullmq": "^5.34.4",
		"otter-core": "workspace:*",
		"otter-db": "workspace:*",
		"otter-jobs": "workspace:*",
		"otter-redis": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "^1.2.11",
		"typescript": "^5.6.3"
	}
}
