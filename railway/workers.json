{
	"$schema": "https://railway.com/railway.schema.json",
	"build": {
		"builder": "RAILPACK",
		"buildCommand": "bun run --cwd packages/db build && bun run --cwd packages/redis build && bun run --cwd packages/jobs build && bun run --cwd packages/core build && bun run tsc -p apps/workers/tsconfig.json --noEmit",
		"watchPatterns": [
			"apps/workers/**",
			"packages/**",
			"package.json",
			"bun.lock"
		]
	},
	"deploy": {
		"startCommand": "WORKERS_HEALTH_PORT=$PORT bun run --cwd apps/workers start",
		"healthcheckPath": "/health",
		"healthcheckTimeout": 300,
		"restartPolicyType": "ON_FAILURE",
		"restartPolicyMaxRetries": 3
	}
}
