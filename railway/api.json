{
	"$schema": "https://railway.com/railway.schema.json",
	"build": {
		"builder": "RAILPACK",
		"buildCommand": "bun run --cwd packages/db build && bun run --cwd packages/redis build && bun run --cwd packages/jobs build && bun run --cwd packages/core build && bun run tsc -p apps/api/tsconfig.json --noEmit",
		"watchPatterns": ["apps/api/**", "packages/**", "package.json", "bun.lock"]
	},
	"deploy": {
		"startCommand": "bun run --cwd apps/api start",
		"healthcheckPath": "/health",
		"healthcheckTimeout": 300,
		"restartPolicyType": "ON_FAILURE",
		"restartPolicyMaxRetries": 3
	}
}
