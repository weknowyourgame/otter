{
	"name": "ai-widget-monorepo",
	"private": true,
	"version": "0.1.0",
	"workspaces": [
		"packages/*",
		"examples/*",
		"apps/*"
	],
	"scripts": {
		"build": "npm run build --workspace=packages/core && npm run build --workspace=packages/sdk",
		"dev": "npm run dev --workspace=apps/web",
		"landing": "npm run dev --workspace=apps/landing"
	}
}
