{
	"name": "ai-widget-standalone",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"description": "Standalone bookmarklet + local server for testing the Nanobrowser-style AI DOM widget on any website.",
	"scripts": {
		"start": "node server.js",
		"test": "node --test \"test/*.test.mjs\""
	},
	"dependencies": {
		"ai-proxy-core": "*"
	}
}
