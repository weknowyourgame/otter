{
	"name": "otto-db",
	"private": true,
	"version": "0.1.0",
	"description": "Session/tenant/knowledge-base persistence for otto-core and apps/api. Postgres via Bun's native SQL client (Bun.SQL) + drizzle-orm/bun-sql — no extra npm dependency, matching how bun:sqlite was used before this was migrated off SQLite. Needs DATABASE_URL pointed at a real Postgres instance.",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist",
		"drizzle"
	],
	"scripts": {
		"build": "tsc -p tsconfig.json --outDir dist",
		"reset": "bun run scripts/reset.ts",
		"db:generate": "drizzle-kit generate",
		"db:baseline": "bun run scripts/baseline-migrations.ts"
	},
	"dependencies": {
		"drizzle-orm": "0.45.2"
	},
	"devDependencies": {
		"@types/bun": "^1.2.11",
		"drizzle-kit": "^0.31.10",
		"typescript": "^5.6.3"
	}
}
